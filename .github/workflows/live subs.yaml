name: Live Subdomain Monitor

on:
  schedule:
    # Runs every 6 hours
    - cron: '0 */6 * * *'
  workflow_dispatch:
    inputs:
      domain:
        description: 'Target domain to scan'
        required: true
        default: 'omnicell.com'
      notify:
        description: 'Send Discord notification even if no changes'
        type: boolean
        default: false

# Prevent overlapping runs for the same domain
concurrency:
  group: subdomain-monitor-${{ github.event.inputs.domain || 'omnicell.com' }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    env:
      DOMAIN: ${{ github.event.inputs.domain || 'omnicell.com' }}
      GO_VERSION: '1.21'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 1

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: false

      - name: Cache Go bin
        uses: actions/cache@v4
        with:
          path: |
            ~/go/bin
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-subdomain-tools-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-subdomain-tools-

      - name: Install subfinder and httpx
        run: |
          go install -v github.com/projectdiscovery/subfinder/v2/cmd/subfinder@latest
          go install -v github.com/projectdiscovery/httpx/cmd/httpx@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      - name: Verify installations
        run: |
          subfinder -version
          httpx -version

      - name: Run subfinder (passive enumeration)
        run: |
          set -euo pipefail
          echo "🔍 Scanning $DOMAIN ..."
          # -all uses all passive sources (no API keys needed, just slower)
          subfinder -d "$DOMAIN" -silent -all -recursive -timeout 30 \
            -o raw-subdomains.txt || true

          if [ ! -s raw-subdomains.txt ]; then
            echo "⚠️ No subdomains found. Exiting gracefully."
            touch current-subdomains.txt
          else
            # Deduplicate, sort, strip whitespace, drop empty lines
            sort -u raw-subdomains.txt | sed '/^[[:space:]]*$/d' > current-subdomains.txt
          fi

          CURRENT_COUNT=$(wc -l < current-subdomains.txt)
          echo "CURRENT_COUNT=$CURRENT_COUNT" >> "$GITHUB_ENV"
          echo "Found $CURRENT_COUNT unique subdomains."

      - name: Check live subdomains with httpx
        run: |
          set -euo pipefail
          echo "🌐 Probing live subdomains for $DOMAIN ..."

          if [ ! -s current-subdomains.txt ]; then
            echo "⚠️ No subdomains to probe. Skipping httpx."
            touch current-live.txt
            echo "LIVE_COUNT=0" >> "$GITHUB_ENV"
            exit 0
          fi

          # Probe each subdomain: HTTP/HTTPS, follow redirects, tech detection
          # -silent suppresses banner, -status-code / -title / -tech-detect enrich output
          httpx -l current-subdomains.txt \
            -silent \
            -follow-redirects \
            -status-code \
            -title \
            -tech-detect \
            -web-server \
            -content-length \
            -timeout 10 \
            -retries 1 \
            -rate-limit 50 \
            -threads 25 \
            -o raw-live.txt || true

          if [ ! -s raw-live.txt ]; then
            echo "⚠️ No live subdomains found."
            touch current-live.txt
          else
            # httpx outputs lines like: https://sub.example.com [200] [Title] [tech]
            # Extract just the URL (first whitespace-separated token) for tracking
            awk '{print $1}' raw-live.txt | sort -u | sed '/^[[:space:]]*$/d' > current-live.txt
          fi

          LIVE_COUNT=$(wc -l < current-live.txt)
          echo "LIVE_COUNT=$LIVE_COUNT" >> "$GITHUB_ENV"
          echo "Found $LIVE_COUNT live subdomains."

      - name: Load previous results
        id: check_previous
        run: |
          if [ -f previous-subdomains.txt ]; then
            echo "previous_exists=true" >> "$GITHUB_OUTPUT"
            echo "PREVIOUS_COUNT=$(wc -l < previous-subdomains.txt)" >> "$GITHUB_ENV"
          else
            echo "previous_exists=false" >> "$GITHUB_OUTPUT"
            echo "PREVIOUS_COUNT=0" >> "$GITHUB_ENV"
          fi

          if [ -f previous-live.txt ]; then
            echo "previous_live_exists=true" >> "$GITHUB_OUTPUT"
            echo "PREVIOUS_LIVE_COUNT=$(wc -l < previous-live.txt)" >> "$GITHUB_ENV"
          else
            echo "previous_live_exists=false" >> "$GITHUB_OUTPUT"
            echo "PREVIOUS_LIVE_COUNT=0" >> "$GITHUB_ENV"
          fi

      - name: Compare with previous results
        if: steps.check_previous.outputs.previous_exists == 'true'
        run: |
          set -euo pipefail
          sort -u current-subdomains.txt > current-sorted.txt
          sort -u previous-subdomains.txt > previous-sorted.txt

          comm -23 current-sorted.txt previous-sorted.txt > new-subdomains.txt
          comm -13 current-sorted.txt previous-sorted.txt > removed-subdomains.txt

          NEW_COUNT=$(wc -l < new-subdomains.txt || echo 0)
          REMOVED_COUNT=$(wc -l < removed-subdomains.txt || echo 0)

          echo "NEW_COUNT=$NEW_COUNT" >> "$GITHUB_ENV"
          echo "REMOVED_COUNT=$REMOVED_COUNT" >> "$GITHUB_ENV"

          echo "✅ New: $NEW_COUNT | ❌ Removed: $REMOVED_COUNT"

      - name: Compare live subdomains with previous
        if: steps.check_previous.outputs.previous_live_exists == 'true'
        run: |
          set -euo pipefail
          sort -u current-live.txt > current-live-sorted.txt
          sort -u previous-live.txt > previous-live-sorted.txt

          comm -23 current-live-sorted.txt previous-live-sorted.txt > new-live.txt
          comm -13 current-live-sorted.txt previous-live-sorted.txt > removed-live.txt

          NEW_LIVE_COUNT=$(wc -l < new-live.txt || echo 0)
          REMOVED_LIVE_COUNT=$(wc -l < removed-live.txt || echo 0)

          echo "NEW_LIVE_COUNT=$NEW_LIVE_COUNT" >> "$GITHUB_ENV"
          echo "REMOVED_LIVE_COUNT=$REMOVED_LIVE_COUNT" >> "$GITHUB_ENV"

          echo "🌐 New Live: $NEW_LIVE_COUNT | 💀 Removed Live: $REMOVED_LIVE_COUNT"

      - name: Initialize counters for first run
        if: steps.check_previous.outputs.previous_exists != 'true'
        run: |
          echo "NEW_COUNT=0" >> "$GITHUB_ENV"
          echo "REMOVED_COUNT=0" >> "$GITHUB_ENV"
          : > new-subdomains.txt
          : > removed-subdomains.txt

      - name: Initialize live counters for first run
        if: steps.check_previous.outputs.previous_live_exists != 'true'
        run: |
          echo "NEW_LIVE_COUNT=0" >> "$GITHUB_ENV"
          echo "REMOVED_LIVE_COUNT=0" >> "$GITHUB_ENV"
          : > new-live.txt
          : > removed-live.txt

      - name: Build job summary
        run: |
          {
            echo "## 🛰️ Subdomain Monitor Report"
            echo ""
            echo "**Domain:** \`${DOMAIN}\`  "
            echo "**Run:** \`${GITHUB_RUN_NUMBER}\`  "
            echo "**Triggered by:** \`${GITHUB_EVENT_NAME}\`  "
            echo "**Timestamp:** \`${{ github.event.repository.updated_at }}\`  "
            echo ""
            echo "### 📋 All Subdomains"
            echo ""
            echo "| Metric | Count |"
            echo "|--------|-------|"
            echo "| Current Subdomains | $CURRENT_COUNT |"
            echo "| Previous Subdomains | $PREVIOUS_COUNT |"
            echo "| 🆕 New | $NEW_COUNT |"
            echo "| ❌ Removed | $REMOVED_COUNT |"
            echo ""
            echo "### 🌐 Live Subdomains"
            echo ""
            echo "| Metric | Count |"
            echo "|--------|-------|"
            echo "| Current Live | $LIVE_COUNT |"
            echo "| Previous Live | $PREVIOUS_LIVE_COUNT |"
            echo "| 🆕 New Live | $NEW_LIVE_COUNT |"
            echo "| 💀 Removed Live | $REMOVED_LIVE_COUNT |"
            echo ""

            if [ "$NEW_COUNT" -gt 0 ]; then
              echo "### 🆕 New Subdomains"
              echo '```'
              head -n 50 new-subdomains.txt
              echo '```'
              echo ""
            fi

            if [ "$REMOVED_COUNT" -gt 0 ]; then
              echo "### ❌ Removed Subdomains"
              echo '```'
              head -n 50 removed-subdomains.txt
              echo '```'
            fi

            if [ -s raw-live.txt ]; then
              echo "### 🌐 Live Subdomains (with details)"
              echo '```'
              head -n 50 raw-live.txt
              echo '```'
            fi

            if [ "$NEW_LIVE_COUNT" -gt 0 ]; then
              echo "### 🆕 New Live Subdomains"
              echo '```'
              head -n 50 new-live.txt
              echo '```'
            fi

            if [ "$REMOVED_LIVE_COUNT" -gt 0 ]; then
              echo "### 💀 Removed Live Subdomains"
              echo '```'
              head -n 50 removed-live.txt
              echo '```'
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Prepare Discord notification
        env:
          SHOULD_NOTIFY_ALL: ${{ github.event.inputs.notify || 'false' }}
        run: |
          set -euo pipefail

          # Decide whether to notify
          HAS_CHANGES=false
          if [ "${{ steps.check_previous.outputs.previous_exists }}" == "true" ] && { [ "$NEW_COUNT" -gt 0 ] || [ "$REMOVED_COUNT" -gt 0 ]; }; then
            HAS_CHANGES=true
          fi
          if [ "${{ steps.check_previous.outputs.previous_live_exists }}" == "true" ] && { [ "$NEW_LIVE_COUNT" -gt 0 ] || [ "$REMOVED_LIVE_COUNT" -gt 0 ]; }; then
            HAS_CHANGES=true
          fi
          if [ "${{ steps.check_previous.outputs.previous_exists }}" != "true" ]; then
            HAS_CHANGES=true
          fi

          if [ "$HAS_CHANGES" != "true" ] && [ "$SHOULD_NOTIFY_ALL" != "true" ]; then
            echo "SKIP_NOTIFICATION=true" >> "$GITHUB_ENV"
            echo "No changes detected — skipping Discord notification."
            exit 0
          fi
          echo "SKIP_NOTIFICATION=false" >> "$GITHUB_ENV"

          # Pick color: green = new found, red = removed only, blue = no change / first run
          COLOR=5814783
          if [ "$NEW_COUNT" -gt 0 ] || [ "$NEW_LIVE_COUNT" -gt 0 ]; then COLOR=3066993; fi
          if [ "$NEW_COUNT" -eq 0 ] && [ "$NEW_LIVE_COUNT" -eq 0 ] && { [ "$REMOVED_COUNT" -gt 0 ] || [ "$REMOVED_LIVE_COUNT" -gt 0 ]; }; then COLOR=15158332; fi

          # Build field values with safe truncation (Discord field limit: 1024 chars)
          build_field() {
            local file="$1" max_lines="${2:-10}" fallback="${3:-None}"
            if [ -s "$file" ]; then
              local out
              out=$(head -n "$max_lines" "$file" | sed 's/^/• /' | jq -Rs .)
              local total
              total=$(wc -l < "$file")
              if [ "$total" -gt "$max_lines" ]; then
                out=$(echo -n "• ... and $((total - max_lines)) more\n" | jq -Rs . | jq -r --arg base "$(echo "$out" | jq -r .)" '$base + .')
                out=$(jq -n --arg v "$out" '$v' )
              fi
              echo "$out"
            else
              echo "$(jq -n --arg v "$fallback" '$v')"
            fi
          }

          CURRENT_LIST=$(head -n 15 current-subdomains.txt | sed 's/^/• /' | jq -Rs .)
          if [ "$CURRENT_COUNT" -gt 15 ]; then
            CURRENT_LIST=$(echo -n "$(echo "$CURRENT_LIST" | jq -r .)\n• ... and $((CURRENT_COUNT - 15)) more" | jq -Rs .)
          elif [ "$CURRENT_COUNT" -eq 0 ]; then
            CURRENT_LIST=$(jq -n --arg v "None" '$v')
          fi

          LIVE_LIST=$(build_field current-live.txt 15 "None")
          NEW_LIST=$(build_field new-subdomains.txt 10 "None")
          REMOVED_LIST=$(build_field removed-subdomains.txt 10 "None")
          NEW_LIVE_LIST=$(build_field new-live.txt 10 "None")
          REMOVED_LIVE_LIST=$(build_field removed-live.txt 10 "None")

          if [ "$PREVIOUS_COUNT" -gt 0 ]; then
            PREV_LIST=$(head -n 15 previous-subdomains.txt | sed 's/^/• /' | jq -Rs .)
            if [ "$PREVIOUS_COUNT" -gt 15 ]; then
              PREV_LIST=$(echo -n "$(echo "$PREV_LIST" | jq -r .)\n• ... and $((PREVIOUS_COUNT - 15)) more" | jq -Rs .)
            fi
          else
            PREV_LIST=$(jq -n --arg v "No previous data" '$v')
          fi

          NOW=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          RUN_URL="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"

          PAYLOAD=$(jq -n \
            --arg title "🛰️ Subdomain Report: $DOMAIN" \
            --arg url "$RUN_URL" \
            --arg current "$CURRENT_COUNT" \
            --arg previous "$PREVIOUS_COUNT" \
            --arg new "$NEW_COUNT" \
            --arg removed "$REMOVED_COUNT" \
            --arg live "$LIVE_COUNT" \
            --arg prevlive "$PREVIOUS_LIVE_COUNT" \
            --arg newlive "$NEW_LIVE_COUNT" \
            --arg removedlive "$REMOVED_LIVE_COUNT" \
            --argjson color "$COLOR" \
            --argjson currentlist "$CURRENT_LIST" \
            --argjson livelist "$LIVE_LIST" \
            --argjson prevlist "$PREV_LIST" \
            --argjson newlist "$NEW_LIST" \
            --argjson removedlist "$REMOVED_LIST" \
            --argjson newlivelist "$NEW_LIVE_LIST" \
            --argjson removedlivelist "$REMOVED_LIVE_LIST" \
            --arg ts "$NOW" \
            --arg footer "Subdomain Monitor • Run #$GITHUB_RUN_NUMBER" \
            '{
              "embeds": [{
                "title": $title,
                "url": $url,
                "color": $color,
                "timestamp": $ts,
                "fields": [
                  {"name": "📊 Current", "value": $current, "inline": true},
                  {"name": "📦 Previous", "value": $previous, "inline": true},
                  {"name": "🌐 Live", "value": $live, "inline": true},
                  {"name": "🆕 New", "value": $new, "inline": true},
                  {"name": "❌ Removed", "value": $removed, "inline": true},
                  {"name": "🆕 New Live", "value": $newlive, "inline": true},
                  {"name": "💀 Removed Live", "value": $removedlive, "inline": true},
                  {"name": "📦 Prev Live", "value": $prevlive, "inline": true},
                  {"name": "Current Subdomains", "value": $currentlist},
                  {"name": "🌐 Live Subdomains", "value": $livelist},
                  {"name": "New Subdomains", "value": $newlist},
                  {"name": "Removed Subdomains", "value": $removedlist},
                  {"name": "New Live Subdomains", "value": $newlivelist},
                  {"name": "Removed Live Subdomains", "value": $removedlivelist}
                ],
                "footer": {"text": $footer}
              }]
            }')

          {
            echo "PAYLOAD<<EOF"
            echo "$PAYLOAD"
            echo "EOF"
          } >> "$GITHUB_ENV"

      - name: Send to Discord
        if: env.SKIP_NOTIFICATION != 'true'
        env:
          WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          if [ -z "$WEBHOOK_URL" ]; then
            echo "⚠️ DISCORD_WEBHOOK_URL secret is not set. Skipping."
            exit 0
          fi
          response=$(curl -sS -w "\n%{http_code}" \
            -H "Content-Type: application/json" \
            -d "$PAYLOAD" \
            "$WEBHOOK_URL")
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | head -n -1)
          echo "HTTP $http_code"
          if [ "$http_code" -ge 400 ]; then
            echo "Discord response: $body"
          fi

      - name: Update previous results
        run: |
          cp -f current-subdomains.txt previous-subdomains.txt
          cp -f current-live.txt previous-live.txt

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add previous-subdomains.txt previous-live.txt
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
            git commit -m "chore: update subdomains + live baseline [${TIMESTAMP}]"
            git push origin HEAD
          fi

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: subdomain-scan-${{ github.run_id }}
          path: |
            current-subdomains.txt
            current-live.txt
            raw-live.txt
            new-subdomains.txt
            removed-subdomains.txt
            new-live.txt
            removed-live.txt
            raw-subdomains.txt
          retention-days: 30
          if-no-files-found: ignore

      - name: Cleanup workspace
        if: always()
        run: |
          rm -f current-subdomains.txt previous-subdomains.txt \
                current-live.txt previous-live.txt \
                new-subdomains.txt removed-subdomains.txt \
                new-live.txt removed-live.txt \
                current-sorted.txt previous-sorted.txt \
                current-live-sorted.txt previous-live-sorted.txt \
                raw-subdomains.txt raw-live.txt
